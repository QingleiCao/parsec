modpath=@PYTHON_SITE_PACKAGES@
if ( -d ${modpath} ) then
  if( $?PYTHONPATH) then
    setenv PYTHONPATH "$modpath:$PYTHONPATH"
  else
    setenv PYTHONPATH "$modpath"
  endif
endif
unset modpath

setenv PATH "@CMAKE_INSTALL_PREFIX@/@PARSEC_INSTALL_BINDIR@:$PATH"

